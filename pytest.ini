[pytest]
testpaths = tests
markers =
    unit: Tests unitarios rapidos (sin modelos)
    integration: Tests de integracion con modelos reales
    slow: Tests que tardan mas de 10 segundos
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
